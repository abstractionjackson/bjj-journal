* [2026-06-13 Sat 16:47]
** DONE Shrink Calendar View
- the current implementation of the calendar is crude, and too
  large. the squares for the date should be small. The whole month
  should be visible in a single viewport. Month and year should be
  visble. Bring in a library, as needed.
- see Cal-Heatmap
** DONE Remove New Column from Date Detail Session List
- the name link handles this
** DONE Update Branding to Link Home
- as a user, I want to click the branding to navigate to the home page
** DONE Replace Session Table Column Time Data with Visualization
- as a user, I do not want to see the time range but a clock with the
  range filled in. 12h time scale. AM/PM should be white clock with
  black fill for AM, black clock with white fill for PM.
- tooltip prints date range on hover
** DONE Remove "Druation" Colunm from Session Table
- as a user, I don't want to see this column
* [2026-06-13 Sat 16:36]
** DONE Add Move Entity
- name: string (unique, PK)
- category: attack | defense | transition
- notes: string
** DONE Update Drill Model to unclude an Array of Moves
- reference Move entries
* [2026-06-13 Sat 16:22]
** DONE Add Search To Session Log
** DONE Add Calendar View
- as a user, I want to see the Recent Sessions section of the home
  page as a calendar of the past six weeks, where dates with a session
  have a distinct color, and provide a link to a date page
** DONE Add Date Page
- as a user, I want dates I click on to render a "date page", that is
  a view with a list of sessions from a given date
 - as a developer, I may want to render data from other indities
   associated with a given date on the page, but for the time being,
   only the sessions need appear.
** DONE Update Session Model to inlcude Name and Position
- The sessions should each have a unique name
- The session should have a number that represents its  position in
  the history. The can be determined by the datetime. (ie. the
  earliest datetime is session #1).
- The positition should be used to generate a default name for the
  session, like Session #N
- Names can be edited, but position is fixed
** DONE Update Session Log Columns
- add Name column
  - click name should go to session
- update Date column
    - date link should route to Date Page (above)
* [2026-06-11 Thu 13:51]
** Production Readiness
*** DONE Refactor as Pure Front End
- migrate from an express API to an api that runs exclusively in the browser
- instead of persisting .json data to disk, write to localStorage
- add an export option
- manage application state in scripts on the browser
- remove auth; application will be unique user
*** DONE Package for Distribution
- add metadata, incl. favicon and opengraph
- The application should be available for download, such that a user
  can clone the code from GitHub, and run the application locally.
- The documentation should include instructions on how to run the application
*** DONE Create an Example Site
- Create an example version of the site that can be deployed to GitHub Pages
- The site should feature realistic mock data for 6 months of
  training
** Deployment
*** DONE Deploy Code to GitHub Pages
- define a GitHub Actions workflow that will build the example site
  for Pages
- create a new GH repo and push the code
* [2026-06-11 Thu 11:47]
** FE
*** Sessions List
**** DONE Add Sessions List Page
- include a page that shows a list of all sessions.
- update the home page to link to this page. plage the link as a
  subheading to "Recent Sessions"
*** Add Long Entry Form
**** DONE Fix Default End Flow
- the current automatic update of the end from initial start date set
  happens after the date is set, but before the time, due to how the
  datetime picker operates. The user chooses a date, and the time in
  the picker defaults to the current time, causing the end datetime to
  update. However, the user needs to set the time before the end
  should update.
- wait until the user has unfocused the start datetime picker before
  updaiting the end datetime
* [2026-06-11 Thu 11:08]
** FrontEnd
*** Log
**** DONE Add Log Entry Form
- default end time: by default the end time should be 1hr from the
  start time. If the start datetime is not set, and the user then sets
  the start, the end should automatically update to one hour later
  than the start (on the same date). if the end time is already set,
  udpates to the start time should not effect the end time value.
*** Detail
**** DONE Style Notes Article to Readable Width
- the notes summary article is not width-limited, and a max-width
  style should be applied that narrows the article to readable
  dimensions.
**** DONE Add Markdown and Org Styling to Notes
- add markdown formatting support
- add Org formatting support
*** Moves List
**** DONE Add Filter/Sort, Search
- Add search with fuzzy support
- add order by date, times drilled, alphabetical move name
- add filter by category, date range
*** Rolls List
**** DONE Add Filter/Sort, Search
- add seach by name
- add sort by rolls, date
- add filter by date range
*** Partner Detail
**** DONE Add Partner Detail page
- include list of links to rolls
** Backend
*** Data Model
**** DONE Parnet
- Add Partner entity
  - name: string
  - id: number or uuid (pk)
- Include a Generic partner, as users may be unable to id their
  partner

