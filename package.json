{
  "name": "bjj-journal",
  "private": true,
  "version": "0.2.0",
  "workspaces": ["client"],
  "scripts": {
    "dev": "npm run dev -w client",
    "build": "npm run build -w client",
    "build:example": "npm run build:example -w client",
    "preview": "npm run preview -w client"
  }
}
